# Nginx reverse proxy (optional)

For production, place TLS certificates and proxy rules here, for example:

- `/` → Vite build or static frontend
- `/api/` → Node backend
- `/ai/` → Python FastAPI service

This folder is intentionally minimal in Phase 1.
