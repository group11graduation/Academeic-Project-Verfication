import { Outlet } from 'react-router-dom';

/** Renders nested class routes (overview, students list, student detail). */
const ClassSectionOutlet = () => <Outlet />;

export default ClassSectionOutlet;
