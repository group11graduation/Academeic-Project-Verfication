# Backend API (no trailing slash) — used at build time for local dev
VITE_API_URL=http://localhost:5000

# On a public server, set PUBLIC_API_URL in docker-compose / Coolify instead.
# The container writes it to public/env-config.js at startup (no rebuild needed).
