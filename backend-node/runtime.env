JWT_SECRET=nHNIwHkJdiQW34il5gvLZG7URaFSC1iwzHy+2q3KHM6OCG1m5KeS05OgiozPAa4W
JWT_REFRESH_SECRET=0fRJDAyrLiYFBmcSCYdYxsNvXzswWsrs4AvEm1TVU3crUZSJ4UAotfieUrIE8O1e
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://173.212.235.206,http://173.212.235.206:5173

# Password reset — public frontend URL (no trailing slash). Must match where users open the app.
FRONTEND_URL=http://173.212.235.206
APP_NAME=Project Verify

# SMTP — fill in your mail provider (Gmail: App Password required)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=Project Verify <noreply@gmail.com>

# Never true in production when SMTP is configured
AUTH_RETURN_RESET_TOKEN=false

# Student project ZIP uploads (250 MB). Raise if Spring/React projects still hit the limit.
MAX_PROJECT_ZIP_BYTES=262144000
MAX_PROJECT_SCREENSHOT_BYTES=5242880
PREVIEW_MAX_EXTRACT_FILES=5000
PREVIEW_MAX_EXTRACT_BYTES=524288000

# Single shared uploads folder at the monorepo root (same as Docker ./uploads).
# Leave unset to use <repo>/uploads automatically. Docker sets UPLOAD_DIR=/app/uploads.
# UPLOAD_DIR=uploads
